CREATE TABLE ASSET (CDCOMPANY SMALLINT NOT NULL, CDNEGOCIATION VARCHAR(15) NOT NULL, PRIMARY KEY ( CDCOMPANY ) );
CREATE TABLE CURRENCY (CDCURRENCY SMALLINT NOT NULL, NMCURRENCY VARCHAR(15) NOT NULL, PRIMARY KEY ( CDCURRENCY ) );
CREATE TABLE ASSETPRICE ( CDPRICE INT NOT NULL, DTPRICE DATE NOT NULL, CDCOMPANY SMALLINT NOT NULL, CDCURRENCY SMALLINT NOT NULL, VLOPEN DECIMAL(10, 2), VLMAX DECIMAL(10, 2), VLLOW DECIMAL(10, 2), VLAVG DECIMAL(10, 2), VLLAST DECIMAL(10, 2), VLHIGHERBUY DECIMAL(10, 2), VLHIGHERSELL DECIMAL(10, 2), QTDAILYTRANS INT, QTTOTALTRANS BIGINT, VLTOTALTITLE DECIMAL(10, 2), PRIMARY KEY ( CDPRICE ) );

ALTER TABLE CURRENCY ALTER COLUMN NMCURRENCY VARCHAR(50);
ALTER TABLE CURRENCY ADD SYMCURRENCY VARCHAR(15);
ALTER TABLE CURRENCY ADD IDCURRENCY VARCHAR(15);

CREATE TABLE CURRENCYPRICE (CDPRICE INT, CDCURRENCY INT, DTPRICE DATE, VLPRICE  DECIMAL(10,2));

CREATE TABLE ADUSER (CDUSER INT, IDUSER VARCHAR(50), PWUSER VARCHAR(50));
INSERT INTO ADUSER (CDUSER, IDUSER, PWUSER) VALUES ('1', 'admin', '111111');